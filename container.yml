---
version: "2"
defaults:
  image: centos:7
  install:
    - bootstrap
    - systemd
    - cleanup
settings:
  project_name: ansible_testing
services:
  node:
    from: "{{ image }}"
    labels:
      maintainer: Wilmar den Ouden <info@wilmardenouden.nl>
    environment:
      container: docker
    command: ['/usr/bin/systemctl']
    roles:
      - role: bootstrap
        when: "'bootstrap' in install"
      - role: systemd
        when: "'systemd' in install"
#      - role: upstart
#        when: "'upstart' in install"
registries: {}
